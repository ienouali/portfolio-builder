import React from "react";
import "./style.scss";

export default function Title() {
    return (
        <div className="title__container">
            <h1>Build your own resume in few minutes fast & quickly with our resume builder</h1>
            <small>Pick a resume template, fill it out, and format. Create a professional resume in a few
                clicks. </small>
        </div>
    );
}
